{
  "name": "krishan99.github.io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal site. Posts live in posts/; run the build to regenerate the homepage and any markdown posts.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
